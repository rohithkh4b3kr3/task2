<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center px-4">

  <div class="bg-gray-800 p-8 rounded-2xl shadow-2xl w-full max-w-md text-white text-center">
    <h1 class="text-3xl font-bold text-green-400 mb-4">Welcome, <%= user.name %> Bro </h1>
    
    <p class="mb-2 text-gray-300"><span class="font-semibold text-white">Username:</span> <%= user.username %></p>
    <p class="mb-6 text-gray-300"><span class="font-semibold text-white">Email:</span> <%= user.email %></p>

    <a href="/logout">
      <button class="bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-6 rounded-lg transition duration-200">
        Logout
      </button>
    </a>
  </div>

</body>
</html>
